#include "../include/Coalition.h"

Coalition::Coalition(int mandates, int first_party) : mandates(mandates), first_party(first_party) 
{
    // You can change the implementation of the constructor, but not the signature!
}
